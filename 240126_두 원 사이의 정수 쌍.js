function solution(r1, r2) {
    var answer = 0;
    // 두 원의 반지름(모두 중심이 원점)
    // 두 원 중 더 큰 원을 찾기 
    // 탐색 범위 : 
    // (r1, 0)~(r2,0) / (0,r1)~(0,r2) / (-r1,0)~(-r2,0) / (0,-r1)~(0,-r2)
    // 
    return answer;
}