function solution(h1, m1, s1, h2, m2, s2) {
    // 시작시간부터 끝나는 시간 사이에 초침/분침/시침이 겹치는 횟수를 카운트
    var answer = -1;
    // 중첩 조건
    // 1. 12시 정각
    // 2. 
    // 3. 1시간 이상 차이가 날 때 시침/분침이 중첩
    return answer;
}